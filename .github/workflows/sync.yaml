name: Sync

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-reuse.yaml
    with:
      workflow: sync.yaml
      remotes: gdrive:Ezlo backup>onedrive-crypt-1:Ezlo backup|gdrive:Ezlo backup>telebox-crypt-1:Ezlo backup
      logger_tags: rclone1|rclone2
    secrets: inherit
